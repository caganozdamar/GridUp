# Bu dosyayi .env olarak kopyalayin: cp .env.example .env

VITE_API_BASE_URL="http://localhost:3000"
# SCADA ekrani: SCADA Gateway'in read-only register ucu (npm run dev:scada).
VITE_SCADA_GATEWAY_URL="http://localhost:1580"
